{# TODO : manage errors display #}

<form class="main-color" method="post">
	<input type="hidden" value="{{ comment.id }}" name="id">
	<label for="comment">Commentaire</label>
	<input type="text" value="{{ comment.comment }}" name="comment" id="comment">
	<input type="hidden" value='{{ session.user.id }}' name="user_id" id="user_id">
	<input type="hidden" value='4' name="training_id" id="training_id">
	{% for error in errors %}
    	<p>{{error}}</p>
	{% endfor %}
	<button>Ajouter/modifier un commentaire</button>
</form>

